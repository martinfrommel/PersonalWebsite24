---
import Monogram from "./Monogram.astro";
import DarkModeToggle from "./DarkModeToggle.astro";
import BottomMenu from "./Nav.astro";
import Tooltip from "./Tooltip.astro";
interface Props {
  className?: string;
}

const { className } = Astro.props;
---

<section class={className}>
  <DarkModeToggle />
  <BottomMenu transition:persist />
  <Tooltip
    tooltipArrow={true}
    tooltipText="Made with 🧗 in 🇨🇿"
    tooltipPosition={"left"}
    tooltipClassName="dark:bg-light dark:text-dark bg-dark text-light font-mono "
    transition:persist><Monogram className="xl:w-24" /></Tooltip
  >
</section>
