---
export interface Props {
  logo: string;
  className?: string;
}

const { logo, className } = Astro.props;

const { default: innerHTML } = await import(`../assets/logos/${logo}.svg?raw`);
---

<Fragment set:html={innerHTML} class:list={[className]} />
